<?php
$title = "Locations";
$this->headTitle($title);
?>
<h1><? echo $this->escapeHtml($title); ?></h1>
Every copy of a book is related to a location, i.e. the room, building or other definition of where the book is located.<br/><br/>

<a class="btn" href="/location/add">Add location</a><br/><br/>
<? if ($locations->count() > 0): ?>
    <table class="table">
        <tr><th>Location</th><th>Action</th></tr>
        <? foreach ($locations as $location): ?>
            <tr>
                <td><? echo $location->getName(); ?></td>
                <td>
                    <a href="/location/edit/<? echo $location->getId(); ?>">Edit</a> | 
                    <a class="delete-location" href="javascript:void(0);" data-href="/location/delete/<? echo $location->getId(); ?>">delete</a>
                </td>
            </tr>
        <? endforeach; ?>
    </table>
<? else: ?>
    <div class="info">No locations has been added</div>
<? endif; ?>

<? echo $this->deleteHelper(); ?>